import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'STUDENT_HUB_MOBILE',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
